# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Secrets / local config
.env

# Bulk data caches — regenerable via `python cli.py asic-load` (do not commit)
data/bulk.duckdb
*.duckdb

*.csv

# Downloaded bulk-source archives (ABR bulk extract zips, ~1GB — do not commit)
data/abn_bulk/

